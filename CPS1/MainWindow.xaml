<Window x:Class="CPS1.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:CPS1"
        mc:Ignorable="d"
        Title="MainWindow"
        Height="610" Width="800">
    <Window.Resources>
        <Storyboard x:Key="showMenu" TargetProperty="margin">
            <ThicknessAnimation From="-100 20 0 0" To="-5 20 0 0" DecelerationRatio="0.8" Duration="0:0:1"/>
        </Storyboard>
        <Storyboard x:Key="hideMenu" TargetProperty="margin">
            <ThicknessAnimation From="-5 20 0 0" To="-100 20 0 0" AccelerationRatio="0.8" Duration="0:0:1"/>
        </Storyboard>
    </Window.Resources>
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="25*" />
            <ColumnDefinition Width="75*" />
        </Grid.ColumnDefinitions>
        <!--<Grid Grid.Column="0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="15*" />
                <ColumnDefinition Width="85*" />
            </Grid.ColumnDefinitions>
            <Grid Margin="0 20 0 0" Grid.Column="0" Width="100">
                <Grid.Effect>
                    <DropShadowEffect BlurRadius="15" ShadowDepth="10"/>
                </Grid.Effect>
                <Grid.RowDefinitions>
                    <RowDefinition Height="50"/>
                    <RowDefinition Height="50"/>
                    <RowDefinition Height="50"/>
                    <RowDefinition Height="50"/>
                </Grid.RowDefinitions>
                <Button Margin="-2 0 0 0" Grid.Row="0" Background="FloralWhite"/>
                <Button Margin="-2 0 0 0" Grid.Row="1" Background="FloralWhite"/>
                <Button Margin="-2 0 0 0" Grid.Row="2" Background="FloralWhite"/>
                <Button Margin="-2 0 0 0" Grid.Row="3" Background="FloralWhite"/>
            </Grid>
        </Grid>-->
            <Grid Grid.Column="1">
            <Grid.RowDefinitions>
                <RowDefinition Height="50*" />
                <RowDefinition Height="50*" />
            </Grid.RowDefinitions>
            <local:SignalChart Grid.Row="0"/>
            <local:HistogramChart Grid.Row="1"/>
        </Grid>
    </Grid>
</Window>
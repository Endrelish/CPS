<UserControl x:Class="CPS1.HistogramChart"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:CPS1"
             xmlns:lvc="clr-namespace:LiveCharts.Wpf;assembly=LiveCharts.Wpf"
             mc:Ignorable="d" 
             d:DesignHeight="305" d:DesignWidth="400">
    <UserControl.Resources>
        <local:ViewModelLocator x:Key="Locator" />
    </UserControl.Resources>
    <Grid DataContext="{Binding ViewModel, Source={StaticResource Locator}}">
        <lvc:CartesianChart>
            <lvc:CartesianChart.Series>
                <lvc:ColumnSeries Values="{Binding Histogram.Values}"/>
            </lvc:CartesianChart.Series>
            <lvc:CartesianChart.AxisX>
                <lvc:Axis Title ="Intervals"  Labels="{Binding Histogram.Labels}"/>
            </lvc:CartesianChart.AxisX>
            <lvc:CartesianChart.AxisY>
                <lvc:Axis Title ="Appearances"  LabelFormatter="{Binding Histogram.Formatter}"/>
            </lvc:CartesianChart.AxisY>
        </lvc:CartesianChart>
    </Grid>
</UserControl>
